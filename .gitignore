# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
# hydra log files
*.log
# hydra output folders
/outputs/
# don't need to track this since it will only store the qdrant collection
/data/
# the .env file might contain sensitive data, so all its keys (that are required) are enumerated in the main.py file
.env
# every time I am running the code for the agent, I am generating a diagram of the agent orchestration (as a sort of sanity check)
src/exploration/fashion_agent_diagram.png
# there is a kaggle.json file which is not needed (was just to download the dataset on google colab)
config/kaggle.json
# I created a file with all the prices from the dataset just to see what their histogram looked like to see how I might implement price based filtering. It is not part of the code.
src/exploration/prices.txt
# I have some random images being generated in this folder
src/exploration/random_images/
# can ignore .chainlit folder for now - it unnecessarily clutters the tracked files with the translations
.chainlit/
# ignore chainlit.md that somehow keeps getting created every run
chainlit.md
